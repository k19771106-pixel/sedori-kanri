# 在庫管理GAS 修正メモ

## 重要な注意点・過去のトラブルと解決策

---

### 1. メルカリShopsのメール送信ドメイン
- **正しいドメイン**: `no-reply@mercari-shops.com`
- **間違い**: `no-reply@shops.mercari.com`
- GmailApp.search() でメールが見つからない場合はドメインを確認する

---

### 2. 商品名の正規表現（メール本文）
メルカリShopsのメール本文は `商品名 ：  値` のようにコロンの前後にスペースがある。

```javascript
// 正しい（\s* を使う）
var nm = body.match(/商品名\s*[：:]\s*(.+)/m);

// 間違い（スペースなしでは一致しない）
var nm = body.match(/商品名[：:](.+)/m);
```

---

### 3. 価格の正規表現（¥記号の文字コード問題）
メール本文の `¥` は特殊な文字コードのため、直接マッチできない。

```javascript
// 正しい（[^\d]* で任意の文字を読み飛ばす）
var pm = body.match(/商品価格[^\d]*([\d,]+)/);

// 間違い（¥が一致しないことがある）
var pm = body.match(/商品価格 ：\s*¥([\d,]+)/);
```

---

### 4. 商品マッピングのキーワード（最重要）
**キーワードは必ずメール本文からコピー＆ペーストすること！**

- `㎜`（特殊文字 U+339C）と `mm`（ASCII文字2つ）は見た目が同じでも**別の文字**
- スプレッドシートに手入力すると文字コードが違う可能性がある
- **対処法**: Gmailのメール本文から直接コピーして、商品マッピングのA列にペースト

---

### 5. トリガーと手動実行の競合
- `checkSalesEmails` は5分おきの自動トリガーが設定されている
- 手動で実行しようとすると「別の実行が進行中のためスキップ」になることがある
- **対処法**: テスト時はトリガーを一時削除 → 手動実行 → テスト後にトリガー再設定

---

### 6. テスト時の手順（必ずこの順番で）
1. 売上管理シートの対象行を**削除**
2. スプレッドシートメニューから**「処理済みフラグをリセット」**を実行
3. トリガーを一時**削除**（手動実行と競合するため）
4. `checkSalesEmails` を**手動実行**
5. 実行ログを確認
6. テスト完了後、トリガーを**再設定**（5分おき）

---

### 7. デバッグ用ログ（一時的に追加するもの）
```javascript
// メール本文確認（行300付近）
Logger.log('BODY: ' + body.substring(0, 500));

// マッピング結果確認（mapProductName後）
Logger.log('MAPPED: ' + mappedName);
```
※ デバッグ完了後は削除してOK

---

### 8. getUnitCost / getShippingCost が空になる場合
- `MAPPED:` ログで返ってくる名前が**生の商品名（長い名前）**のままなら、マッピング失敗
- 原因1: キーワードの文字コードが違う → 上記4番参照
- 原因2: 在庫サマリーのB列の商品名とマッピングのB列が一致していない
- 原因3: 存在する行が見つかってupdateRowStatusだけ呼ばれている → 行を削除してリセット
