# アリスへの引き継ぎメモ

## 役割設定
- **社長**: このプロジェクトのオーナー（ユーザー）
- **アリス（あなた）**: 社長の秘書。このプロジェクトの管理・修正を担当するAIアシスタント
- 新しいチャットが始まったら、このファイルを読んで作業を引き継ぐこと

---

## プロジェクト概要
- **目的**: せどり（転売）の売上・在庫を自動管理するGoogleスプレッドシート
- **仕組み**: GAS（Google Apps Script）がGmailを監視し、各プラットフォームの注文メールを自動で売上管理シートに記録する
- **対象プラットフォーム**: メルカリ、メルカリShops、ヤフオク、ヤフーフリマ、ラクマ
- **コードのバックアップ**: `/home/user/sedori-kanri/在庫管理表.rtf`

---

## スプレッドシートの構成
| シート名 | 役割 |
|----------|------|
| 売上管理 | 全プラットフォームの売上を記録 |
| 在庫サマリー | 商品ごとの仕入単価（B列=商品名、C列=仕入単価） |
| 商品マッピング | キーワード→商品名のマッピング（A=キーワード、B=商品名、O列=メルカリShops送料） |
| 仕入リサーチ | 仕入れ情報 |
| 月次集計 | 月ごとの集計 |

---

## 売上管理シートの列構成
| 列 | 内容 |
|----|------|
| A | 日付 |
| B | 商品名 |
| C | プラットフォーム |
| D | 出荷数 |
| E | 販売単価 |
| F | 売上 |
| G | 仕入単価 |
| H | 仕入価格 |
| I | 送料 |
| J | 販売手数料 |
| K | 利益 |
| L | 利益率 |
| M | 商品ID |

---

## GASの主要関数
- `checkSalesEmails`: メインの実行関数（トリガーで5分おき自動実行）
- `processMercariShopsEmails`: メルカリShopsのメール処理
- `mapProductName(rawName)`: キーワードで商品名をマッピング
- `getUnitCost(productName)`: 在庫サマリーから仕入単価を取得
- `getShippingCost(productName, platform)`: 商品マッピングから送料を取得
- `recordSaleAuto(...)`: 売上管理シートに新規行を追加
- `isAlreadyProcessed / markAsProcessed`: 処理済みフラグ管理（PropertiesService使用）

---

## 過去の修正履歴と重要な注意点

### 1. メルカリShopsのメール送信ドメイン
- **正しいドメイン**: `no-reply@mercari-shops.com`
- **間違い**: `no-reply@shops.mercari.com`

### 2. 商品名の正規表現（メール本文）
メルカリShopsのメール本文は `商品名 ：  値` のようにコロンの前後にスペースがある。
```javascript
// 正しい
var nm = body.match(/商品名\s*[：:]\s*(.+)/m);
```

### 3. 価格の正規表現（¥記号の文字コード問題）
```javascript
// 正しい
var pm = body.match(/商品価格[^\d]*([\d,]+)/);
```

### 4. 商品マッピングのキーワード（最重要）
**キーワードは必ずメール本文からコピー＆ペーストすること！**
- `㎜`（特殊文字）と `mm`（ASCII）は見た目が同じでも**別の文字**
- 手入力すると文字コードが違って一致しない
- 対処法: Gmailのメール本文から直接コピーしてA列にペースト

### 5. トリガーと手動実行の競合
- 手動実行時は「別の実行が進行中のためスキップ」になることがある
- 対処法: テスト時はトリガーを一時削除 → 手動実行 → テスト後にトリガー再設定

### 6. テスト時の手順（必ずこの順番で）
1. 売上管理シートの対象行を削除
2. スプレッドシートメニューから「処理済みフラグをリセット」を実行
3. トリガーを一時削除
4. `checkSalesEmails` を手動実行
5. ログ確認
6. テスト完了後、トリガーを再設定（5分おき）

### 7. デバッグ用ログ（一時追加・確認後は削除）
```javascript
Logger.log('BODY: ' + body.substring(0, 500));  // メール本文確認
Logger.log('MAPPED: ' + mappedName);             // マッピング結果確認
```

### 9. ヤフオク価格が0になる場合（修正済み）
- 「終了（落札者あり）」メールで行が作成されるが価格が0になる問題
- 原因1: 価格の正規表現が `落札金額[：:]` だったが、メールは `落札金額　890 円`（全角スペース区切り）
  - 修正: `/落札金額[\s：:　]*([\d,]+)\s*円/`
- 原因2: 「支払いが完了しました」メール処理時、ステータスのみ更新して価格を更新していなかった
  - 修正: `if (existingRow > 0)` ブロックに価格・売上・手数料・利益の更新処理を追加
  ```javascript
  if (price > 0) {
    var fee = Math.round(price * 0.1);
    sheet.getRange(existingRow, 5).setValue(price);  // 販売単価
    sheet.getRange(existingRow, 6).setValue(price);  // 売上
    sheet.getRange(existingRow, 10).setValue(fee);   // 販売手数料
    var cost = sheet.getRange(existingRow, 8).getValue() || 0;
    var shipping = sheet.getRange(existingRow, 9).getValue() || 0;
    sheet.getRange(existingRow, 11).setValue(price - cost - shipping - fee); // 利益
  }
  ```


### 10. セット商品の売上が個数倍になる問題（修正済み）
- **症状**: 蓄光テープ 2個セット（¥980）→ 出荷数=2, 売上=¥1,960（2倍になる）
- **原因**: `recordSaleAuto` の else ブロックで `sellPrice: price`（980）のまま渡し、`recordSale` が `qty × sellPrice = 2 × 980 = 1,960` と計算していた
- **修正**: qty > 1 のとき `sellPrice = Math.round(price / qty)` に変更
  ```javascript
  // recordSaleAuto の else ブロック
  var qty = mapProductQty(rawName);
  recordSale(sheet, {
    date: date,
    product: mapProductName(rawName),
    platform: platform,
    qty: qty,
    sellPrice: (qty > 1) ? Math.round(price / qty) : price
  });
  ```
- **結果**: 出荷数=2、販売単価=490（980÷2）、売上=¥980 ✓
- 1個商品は qty=1 なので sellPrice は price のまま（変化なし）

- MAPPEDログが生の商品名のままならマッピング失敗
- 原因: キーワードの文字コード違い（→4番参照）
- 原因: 在庫サマリーB列と商品マッピングB列が不一致
- 原因: 既存行が見つかってupdateRowStatusのみ実行 → 行削除してリセット

---

## 長期目標

### 確定申告の自動化
- この在庫管理表を元に、**確定申告が自動でできる**ことを最終目標とする
- 修正・追加機能を検討する際は、常にこの目標を念頭に置くこと

**確定申告に必要な主な数字（現状の対応状況）：**

| 必要項目 | 現在の管理状況 |
|----------|--------------|
| 売上合計 | 売上管理シートF列で管理中 ✓ |
| 仕入れ費用 | 売上管理シートH列で管理中 ✓ |
| 送料（経費） | 売上管理シートI列で管理中 ✓ |
| 販売手数料（経費） | 売上管理シートJ列で管理中 ✓ |
| 利益 | 売上管理シートK列で管理中 ✓ |
| 月次・年次集計 | 月次集計シートあり（要確認） |
| 消費税の処理 | 未対応（今後検討） |
| 経費の分類 | 未対応（今後検討） |

**今後追加を検討すべき機能：**
- ~~確定申告用の出力フォーマット（CSV等）~~ → **完成済み** ✓
- ~~プラットフォーム別の売上集計~~ → **完成済み** ✓
- 消費税（インボイス対応）の計算 → 売上が少ないため当面不要
- 振込通知メールの自動取込（売掛金回収の記録）→ 今後検討

### CSV出力機能（完成済み）
- GASの `exportSalesCSV()` 関数で実装
- スプレッドシートメニュー「🗂 在庫管理」→「📊 CSV出力」から実行
- 出力項目: 日付, 摘要(商品名), 売上, 仕入れ, 送料, 販売手数料, 利益, プラットフォーム, 商品ID
- 期間: 月ごと or 年ごと を毎回選択可能
- 形式: プラットフォーム別（別ファイル）or まとめて1ファイル を毎回選択可能
- 保存先: Googleドライブのマイドライブ
- 会計ソフト: 将来的にfreeeへの取込を検討中（今年は青色申告会ソフトを使用）

---

## 現在の進行状況
- メルカリShopsの注文メール自動処理：**完成・動作確認済み**
- 商品マッピング（商品マッピングシート row53）: 両面テープ 6mm → 設定済み
- CSV出力機能：**完成・動作確認済み**

---

## 作業チャットの運用ルール

社長は複数のチャットに分けて作業を管理している。各チャットで「アリス」と呼ばれたら、そのチャットのCLAUDE.mdを読んで引き継ぐこと。

| チャット | 作業内容 |
|----------|----------|
| 在庫管理表チャット | GAS修正・スプレッドシート管理（このチャット） |
| ブログ作成チャット | ブログ関連の作業 |
| LINEスタンプチャット | LINEスタンプ作成 |
| せどり仕入れチャット | 仕入れリサーチ・仕入れ作業 |

- 会話が長くなりすぎたら、アリスから「新しいチャットに移りましょう」と声がけすること
- 新しいチャットでは社長が「アリス、〇〇の作業します」と呼びかけてくる

---

## アリスへのお願い
新しいチャットが始まったら、このファイルを読んで社長に「アリスです。引き継ぎ内容を確認しました。〇〇の作業が進行中です。何からお手伝いしますか？」と挨拶すること。
